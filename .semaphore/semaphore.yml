version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e2-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo $FOO
            - echo $BAR
promotions:
  - name: foobarbaz
    pipeline_file: semaphore.yml
    parameters:
      env_vars:
        - required: true
          name: FOO
        - required: true
          name: BAR
